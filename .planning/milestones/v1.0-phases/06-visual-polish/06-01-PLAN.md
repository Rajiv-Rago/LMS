---
phase: 06-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/Button.tsx
  - components/ui/EmptyState.tsx
  - components/ui/BottomNav.tsx
  - app/(dashboard)/layout.tsx
  - package.json
autonomous: true
requirements: [VISL-02, VISL-03]
must_haves:
  truths:
    - "A shared Button component renders primary, secondary, danger, and ghost variants with consistent rounded-lg styling"
    - "A shared EmptyState component renders an icon, title, description, and optional action button"
    - "Mobile users see a fixed bottom navigation bar with Home, Explore, Courses, and Me tabs"
    - "Bottom nav is hidden on desktop (lg: breakpoint and above)"
    - "Dashboard layout spinner is replaced with a skeleton that mimics sidebar + empty main area"
    - "Main content area has bottom padding on mobile to clear the bottom nav"
  artifacts:
    - path: "components/ui/Button.tsx"
      provides: "Shared button with variant/size system"
      exports: ["default"]
    - path: "components/ui/EmptyState.tsx"
      provides: "Shared empty state with icon, title, description, action"
      exports: ["default"]
    - path: "components/ui/BottomNav.tsx"
      provides: "Mobile bottom navigation bar"
      exports: ["default"]
  key_links:
    - from: "app/(dashboard)/layout.tsx"
      to: "components/ui/BottomNav.tsx"
      via: "import and render"
      pattern: "import.*BottomNav"
    - from: "components/ui/EmptyState.tsx"
      to: "components/ui/Button.tsx"
      via: "import for action button"
      pattern: "import.*Button"
---

<objective>
Create the foundation UI components (Button, EmptyState, BottomNav) and wire them into the dashboard layout. This establishes the building blocks that all subsequent page-polish plans depend on.

Purpose: Every subsequent plan needs Button for style replacement, EmptyState for empty page states, and BottomNav is a layout-level change that must happen once. The layout spinner replacement demonstrates the skeleton pattern.
Output: Three new components, modified dashboard layout, lucide-react installed.
</objective>

<execution_context>
@/home/rajiv/.claude/get-shit-done/workflows/execute-plan.md
@/home/rajiv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-visual-polish/06-CONTEXT.md
@.planning/phases/06-visual-polish/06-RESEARCH.md

@components/ui/Skeleton.tsx
@app/(dashboard)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lucide-react and create Button, EmptyState components</name>
  <files>package.json, components/ui/Button.tsx, components/ui/EmptyState.tsx</files>
  <action>
1. Install lucide-react: `npm install lucide-react`

2. Create `components/ui/Button.tsx`:
   - forwardRef-based component with ButtonHTMLAttributes
   - variant prop: primary (`bg-indigo-600 text-white hover:bg-indigo-500 disabled:bg-indigo-400`), secondary (`bg-zinc-100 dark:bg-zinc-800 text-zinc-700 dark:text-zinc-300 hover:bg-zinc-200 dark:hover:bg-zinc-700`), danger (`bg-red-600 text-white hover:bg-red-500`), ghost (`text-zinc-600 dark:text-zinc-400 hover:bg-zinc-100 dark:hover:bg-zinc-800`)
   - size prop: sm (`px-3 py-1.5 text-sm`), default (`px-4 py-2 text-sm`)
   - All buttons: `rounded-lg font-medium disabled:opacity-50 disabled:cursor-not-allowed transition-colors`
   - Default variant: primary, default size: default
   - className prop merges with computed classes (appended, so user can override)

3. Create `components/ui/EmptyState.tsx`:
   - Props: icon (LucideIcon type), title (string), description (optional string), action (optional {label: string, onClick: () => void})
   - Layout: flex column, centered, py-12
   - Icon: h-12 w-12 text-zinc-300 dark:text-zinc-600 strokeWidth={1.5} mb-4
   - Title: text-base font-semibold text-zinc-900 dark:text-white
   - Description: mt-1 text-sm text-zinc-500 dark:text-zinc-400 max-w-sm
   - Action: Button variant="primary" size="sm" mt-4
   - Import Button from ./Button
  </action>
  <verify>
    <automated>cd /home/rajiv/Personal/Projects/active/LMS && node -e "require('lucide-react')" && echo "lucide-react OK" && npx tsc --noEmit components/ui/Button.tsx components/ui/EmptyState.tsx 2>&1 | head -20 || echo "Type check done"</automated>
  </verify>
  <done>lucide-react installed, Button.tsx exports a forwardRef component with 4 variants and 2 sizes, EmptyState.tsx exports a component that uses Button and accepts a LucideIcon</done>
</task>

<task type="auto">
  <name>Task 2: Create BottomNav and update dashboard layout</name>
  <files>components/ui/BottomNav.tsx, app/(dashboard)/layout.tsx</files>
  <action>
1. Create `components/ui/BottomNav.tsx`:
   - "use client" component
   - Import Link from next/link, usePathname from next/navigation
   - Import icons from lucide-react: Home, Compass, BookOpen, User
   - 4 nav items: Home (/dashboard, Home icon), Explore (/explore, Compass icon), Courses (/courses, BookOpen icon), Me (/profile, User icon)
   - Fixed bottom nav: `fixed bottom-0 left-0 right-0 z-50 bg-white dark:bg-zinc-900 border-t border-zinc-200 dark:border-zinc-800 lg:hidden`
   - Inner container: `flex items-center justify-around h-16`
   - Each link: `flex flex-col items-center justify-center min-w-[44px] min-h-[44px] gap-1 text-xs font-medium transition-colors`
   - Active state: `text-indigo-600 dark:text-indigo-400` (match by pathname === href or pathname.startsWith(href + "/"))
   - Inactive: `text-zinc-500 dark:text-zinc-400`
   - Icon size: h-5 w-5
   - aria-label="Mobile navigation" on nav element

2. Modify `app/(dashboard)/layout.tsx`:
   - Import BottomNav from @/components/ui/BottomNav
   - Import Skeleton from @/components/ui/Skeleton
   - Replace the loading spinner (lines 57-63) with a skeleton layout that mimics the sidebar + empty main area:
     - Full min-h-screen container with bg-zinc-50 dark:bg-zinc-950
     - Hidden sidebar placeholder (lg:block only): w-64 fixed left-0 inset-y-0, with Skeleton blocks for logo area (h-16 border-b) and 5 nav items (h-8 each with space-y-2 in px-4 py-4)
     - Main area: lg:pl-64 pt-14 lg:pt-0 with p-4 lg:p-6, containing a few Skeleton blocks (h-8 w-48 for title, then h-64 w-full for content area)
   - Add BottomNav after the main element, before closing div
   - Change main content padding: `p-6` becomes `p-4 lg:p-6` (mobile-first spacing)
   - Add bottom padding for bottom nav clearance: add `pb-16 lg:pb-0` to main element
   - Final main: `<main id="main-content" className="lg:pl-64 pt-14 lg:pt-0 pb-16 lg:pb-0">`
   - Final content div: `<div className="p-4 lg:p-6">{children}</div>`
  </action>
  <verify>
    <automated>cd /home/rajiv/Personal/Projects/active/LMS && npx tsc --noEmit app/\(dashboard\)/layout.tsx 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>BottomNav component renders 4 tabs with 44px touch targets and active state highlighting, dashboard layout uses skeleton loading state instead of spinner, main content has responsive padding (p-4 mobile / p-6 desktop) and bottom padding for nav clearance, BottomNav hidden on lg: and above</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Button.tsx, EmptyState.tsx, BottomNav.tsx all exist in components/ui/
- Dashboard layout imports and renders BottomNav
- Dashboard layout loading state is a skeleton (no animate-spin)
- Main content padding is p-4 lg:p-6 with pb-16 lg:pb-0
</verification>

<success_criteria>
- Three new UI components created with dark mode support
- Dashboard layout uses skeleton loading instead of spinner
- Mobile bottom nav visible at < 1024px with 4 tabs
- Bottom nav hidden on desktop
- Content area has responsive padding and bottom nav clearance
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-polish/06-01-SUMMARY.md`
</output>
