---
phase: 06-visual-polish
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - app/(dashboard)/dashboard/loading.tsx
  - app/(dashboard)/dashboard/page.tsx
  - app/(dashboard)/courses/loading.tsx
  - app/(dashboard)/courses/page.tsx
  - app/(dashboard)/profile/loading.tsx
  - app/(dashboard)/profile/page.tsx
  - app/(dashboard)/settings/loading.tsx
  - app/(dashboard)/settings/page.tsx
autonomous: true
requirements: [VISL-01, VISL-02, VISL-03]
must_haves:
  truths:
    - "Navigating to /dashboard shows a page-shaped skeleton during route transition"
    - "Dashboard page data loading shows the same skeleton pattern inline (no spinner)"
    - "Navigating to /courses shows a skeleton with search bar and card grid placeholders"
    - "Navigating to /profile shows a skeleton mimicking the profile form layout"
    - "Navigating to /settings shows a skeleton mimicking the settings form layout"
    - "All four pages use text-2xl font-bold for page titles, text-lg font-semibold for section headings"
    - "All four pages use p-4 lg:p-6 compatible spacing with space-y-6 sections"
    - "All buttons on these pages use the shared Button component"
    - "Empty states on dashboard and courses use the shared EmptyState component"
  artifacts:
    - path: "app/(dashboard)/dashboard/loading.tsx"
      provides: "Dashboard route-level skeleton"
    - path: "app/(dashboard)/courses/loading.tsx"
      provides: "Courses list route-level skeleton"
    - path: "app/(dashboard)/profile/loading.tsx"
      provides: "Profile route-level skeleton"
    - path: "app/(dashboard)/settings/loading.tsx"
      provides: "Settings route-level skeleton"
  key_links:
    - from: "app/(dashboard)/dashboard/page.tsx"
      to: "components/ui/Button.tsx"
      via: "import for all button elements"
      pattern: "import.*Button.*from.*components/ui/Button"
    - from: "app/(dashboard)/courses/page.tsx"
      to: "components/ui/EmptyState.tsx"
      via: "import for no-courses state"
      pattern: "import.*EmptyState"
---

<objective>
Polish the four primary dashboard navigation pages: Dashboard, Courses list, Profile, and Settings. Add route-level loading.tsx skeletons, upgrade inline loading states from spinners to page-mimicking skeletons, apply the typography/spacing scale, and replace all inline button styles with the shared Button component.

Purpose: These are the most-visited pages (every session touches dashboard and courses). Polishing them first creates the strongest impression of consistency.
Output: 4 loading.tsx files, 4 updated page files with consistent skeleton/typography/spacing/buttons.
</objective>

<execution_context>
@/home/rajiv/.claude/get-shit-done/workflows/execute-plan.md
@/home/rajiv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-visual-polish/06-CONTEXT.md
@.planning/phases/06-visual-polish/06-RESEARCH.md
@.planning/phases/06-visual-polish/06-01-SUMMARY.md

@components/ui/Skeleton.tsx
@app/(dashboard)/dashboard/page.tsx
@app/(dashboard)/courses/page.tsx
@app/(dashboard)/profile/page.tsx
@app/(dashboard)/settings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx skeletons for dashboard, courses, profile, settings</name>
  <files>app/(dashboard)/dashboard/loading.tsx, app/(dashboard)/courses/loading.tsx, app/(dashboard)/profile/loading.tsx, app/(dashboard)/settings/loading.tsx</files>
  <action>
Create page-specific loading.tsx files using Skeleton primitives from @/components/ui/Skeleton. Each must mimic the actual page layout. These are server components (no "use client").

1. `app/(dashboard)/dashboard/loading.tsx` -- DashboardLoading:
   - space-y-6 container
   - Generation input area: rounded-lg border border-zinc-200 dark:border-zinc-800 p-4, Skeleton h-6 w-48 mb-4, Skeleton h-10 w-full
   - "My Courses" section: Skeleton h-5 w-32 mb-4, grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3 with 3 SkeletonCards
   - "Enrolled Courses" section: same pattern with 2 SkeletonCards

2. `app/(dashboard)/courses/loading.tsx` -- CoursesLoading:
   - space-y-6 container
   - Page title: Skeleton h-8 w-40
   - Search bar: Skeleton h-10 w-full rounded-lg
   - Card grid: grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3 with 6 SkeletonCards

3. `app/(dashboard)/profile/loading.tsx` -- ProfileLoading:
   - space-y-6 container
   - Page title: Skeleton h-8 w-32
   - Form card: rounded-lg border border-zinc-200 dark:border-zinc-800 p-4 space-y-4
     - 3 form fields: each is Skeleton h-4 w-24 (label) + Skeleton h-10 w-full (input)
   - Button area: Skeleton h-10 w-24

4. `app/(dashboard)/settings/loading.tsx` -- SettingsLoading:
   - space-y-6 container
   - Page title: Skeleton h-8 w-32
   - Settings card: rounded-lg border border-zinc-200 dark:border-zinc-800 p-4 space-y-4
     - 3 setting rows: Skeleton h-4 w-32 + Skeleton h-10 w-full
   - Button: Skeleton h-10 w-24
  </action>
  <verify>
    <automated>cd /home/rajiv/Personal/Projects/active/LMS && ls app/\(dashboard\)/dashboard/loading.tsx app/\(dashboard\)/courses/loading.tsx app/\(dashboard\)/profile/loading.tsx app/\(dashboard\)/settings/loading.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>4 loading.tsx files exist, each mimics its page's actual layout using Skeleton primitives, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Polish dashboard, courses, profile, settings pages (skeletons, typography, spacing, buttons, empty states)</name>
  <files>app/(dashboard)/dashboard/page.tsx, app/(dashboard)/courses/page.tsx, app/(dashboard)/profile/page.tsx, app/(dashboard)/settings/page.tsx</files>
  <action>
For each of the 4 pages, apply these changes:

**A. Replace inline loading spinners with page-mimicking skeletons:**
- Find the `if (loading)` or `if (!data)` block that currently shows a spinner or simple skeleton
- Replace with a skeleton that matches the loading.tsx pattern for that page (consistent skeleton across route transition and data fetch)
- Use Skeleton, SkeletonText, SkeletonCard, SkeletonTable from @/components/ui/Skeleton
- Remove any `animate-spin rounded-full` spinner patterns

**B. Apply typography scale consistently:**
- Page titles: `text-2xl font-bold text-zinc-900 dark:text-white`
- Section headings: `text-lg font-semibold text-zinc-900 dark:text-white`
- Card titles: `text-base font-semibold`
- Body text: `text-sm`
- Captions/meta: `text-xs text-zinc-500 dark:text-zinc-400`
- Fix any text-xl or text-3xl page titles to text-2xl
- Fix any inconsistent heading sizes

**C. Apply spacing scale:**
- Page-level spacing: space-y-6 between major sections
- Card/grid gaps: gap-4
- Inner card padding: p-4
- These pages inherit the layout's p-4 lg:p-6 page padding

**D. Replace inline button styles with Button component:**
- Import Button from @/components/ui/Button
- Replace all `<button className="...bg-indigo-600...">` with `<Button variant="primary">`
- Replace cancel/secondary buttons with `<Button variant="secondary">`
- Replace delete/destructive buttons with `<Button variant="danger">`
- Replace text/ghost buttons with `<Button variant="ghost">`
- For `<Link>` elements styled as buttons: keep as Link but apply matching Tailwind classes from the Button component (rounded-lg font-medium + variant colors) -- do NOT wrap Link in Button

**E. Add EmptyState components where applicable:**
- Dashboard "My Courses" empty: use EmptyState with BookOpen icon, "No courses yet", "Generate your first course to get started", action: {label: "Generate Course", onClick: focus the topic input}
- Dashboard "Enrolled Courses" empty: use EmptyState with Compass icon, "No enrollments", "Browse the course catalog to find courses"
- Courses page empty: use EmptyState with BookOpen icon, "No courses", "You haven't created or enrolled in any courses yet"
- Import EmptyState from @/components/ui/EmptyState and icons from lucide-react

**F. Mobile responsiveness:**
- Ensure all grids use grid-cols-1 on mobile (default), stepping up at md: or lg:
- Tables on profile/settings: stack on mobile if applicable, or allow horizontal scroll with overflow-x-auto
- Buttons: ensure min-h-[44px] on mobile via responsive sizing or padding
  </action>
  <verify>
    <automated>cd /home/rajiv/Personal/Projects/active/LMS && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 pages have consistent typography (text-2xl titles, text-lg sections), consistent spacing (space-y-6, gap-4, p-4), all buttons use the shared Button component, empty states use EmptyState with Lucide icons, no spinning circle loaders remain on any of these pages</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- No `animate-spin` or `border-b-2 border-indigo-600` patterns in the 4 page files
- All 4 pages import Button from @/components/ui/Button
- 4 loading.tsx files exist and export default functions
- grep for "text-2xl font-bold" in page titles across all 4 files
</verification>

<success_criteria>
- Route transitions to dashboard/courses/profile/settings show page-shaped skeletons
- Data loading on each page shows matching skeleton (no spinners)
- Typography is consistent: text-2xl titles, text-lg sections, text-sm body
- All buttons use shared Button component with appropriate variants
- Empty states show icon + text + action using EmptyState component
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-polish/06-02-SUMMARY.md`
</output>
