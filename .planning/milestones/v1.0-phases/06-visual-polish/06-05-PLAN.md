---
phase: 06-visual-polish
plan: 05
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - app/(public)/explore/loading.tsx
  - app/(public)/explore/page.tsx
  - app/(public)/courses/[id]/loading.tsx
  - app/(public)/courses/[id]/page.tsx
  - app/page.tsx
  - app/(auth)/login/page.tsx
  - app/(auth)/register/page.tsx
  - app/(auth)/forgot-password/page.tsx
  - app/(auth)/reset-password/page.tsx
autonomous: true
requirements: [VISL-01, VISL-02, VISL-03]
must_haves:
  truths:
    - "Navigating to /explore shows a catalog-shaped skeleton during route transition"
    - "Navigating to a public course page shows a course detail skeleton"
    - "Public pages and dashboard pages share the same visual style (no distinct marketing feel)"
    - "Home page, auth pages use consistent typography and spacing"
    - "All interactive elements across public and auth pages have 44px touch targets on mobile"
    - "No spinning circle loaders remain anywhere in the app"
  artifacts:
    - path: "app/(public)/explore/loading.tsx"
      provides: "Explore catalog route skeleton"
    - path: "app/(public)/courses/[id]/loading.tsx"
      provides: "Course preview route skeleton"
  key_links:
    - from: "app/(public)/explore/page.tsx"
      to: "components/ui/Button.tsx"
      via: "import for catalog buttons"
      pattern: "import.*Button.*from.*components/ui/Button"
    - from: "app/page.tsx"
      to: "components/ui/Button.tsx"
      via: "import or matching className for CTA buttons"
      pattern: "rounded-lg font-medium"
---

<objective>
Polish the public-facing pages (Explore catalog, Course preview), the landing home page, and all auth pages. Add loading.tsx skeletons to public routes, apply typography/spacing/button consistency, and ensure mobile responsiveness across all pages that users see before and after login.

Purpose: These pages form the first impression (home, auth) and the discovery experience (explore, course preview). Consistent visual style between public and dashboard pages per user decision -- no distinct "marketing" feel.
Output: 2 loading.tsx files, 7 updated page files, complete app-wide visual consistency.
</objective>

<execution_context>
@/home/rajiv/.claude/get-shit-done/workflows/execute-plan.md
@/home/rajiv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-visual-polish/06-CONTEXT.md
@.planning/phases/06-visual-polish/06-RESEARCH.md
@.planning/phases/06-visual-polish/06-01-SUMMARY.md

@components/ui/Skeleton.tsx
@components/ui/Button.tsx
@components/ui/EmptyState.tsx
@app/(public)/explore/page.tsx
@app/(public)/courses/[id]/page.tsx
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx skeletons for public routes and polish explore/course preview pages</name>
  <files>app/(public)/explore/loading.tsx, app/(public)/explore/page.tsx, app/(public)/courses/[id]/loading.tsx, app/(public)/courses/[id]/page.tsx</files>
  <action>
Read each page first to understand current structure.

**Loading files (server components, no "use client"):**

1. `app/(public)/explore/loading.tsx` -- ExploreLoading:
   - space-y-6 container
   - Skeleton h-10 w-64 (page title)
   - Skeleton h-10 w-full (search bar)
   - Grid: grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3, 6 catalog card skeletons. Each card: rounded-lg border p-0 with Skeleton aspect-video w-full (image placeholder) + div p-4 space-y-2 with Skeleton h-5 w-2/3 + Skeleton h-4 w-full + Skeleton h-4 w-1/2
   - Note: the Explore page has its own local SkeletonCard with aspect-video -- keep the local skeleton definition in the page for inline loading if it exists, but create the loading.tsx to match it

2. `app/(public)/courses/[id]/loading.tsx` -- CoursePreviewLoading:
   - space-y-6 container
   - Skeleton h-10 w-2/3 (course title)
   - Skeleton h-4 w-32 (instructor info)
   - SkeletonText lines=3 (description)
   - Skeleton h-12 w-40 (enroll button)
   - Skeleton h-6 w-32 (syllabus heading)
   - space-y-2, 5x Skeleton h-12 w-full (module accordion items)

**Page polish:**

3. `app/(public)/explore/page.tsx`:
   - Apply typography scale: page title text-2xl font-bold, card titles text-base font-semibold, body text-sm, meta text-xs text-zinc-500
   - Apply spacing: space-y-6 between search and grid, gap-4 in grid, p-4 in cards
   - Replace inline button styles with Button component where applicable (search button, load more button)
   - Empty state: use EmptyState with Search icon for no search results ("No courses found", "Try different search terms")
   - Ensure cards are mobile-friendly: grid-cols-1 default, sm:grid-cols-2, lg:grid-cols-3
   - Card images/placeholders: aspect-video for consistent card heights

4. `app/(public)/courses/[id]/page.tsx`:
   - This is likely a thin server component that imports CoursePreview client component. Read the actual CoursePreview component (likely in components/ or inline).
   - If CoursePreview is a separate component file, apply the polish there:
     - Typography scale for course title, description, module headings
     - Spacing scale
     - Button component for "Enroll" CTA and any other buttons
     - Mobile: enroll button full-width on mobile (w-full sm:w-auto), syllabus accordion full-width
     - Replace any spinner loading states with skeletons
  </action>
  <verify>
    <automated>cd /home/rajiv/Personal/Projects/active/LMS && ls app/\(public\)/explore/loading.tsx app/\(public\)/courses/\[id\]/loading.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>2 loading.tsx files exist for public routes, explore page has consistent typography/spacing/buttons with EmptyState for no results, course preview has consistent styling, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Polish home page and auth pages (typography, spacing, buttons, mobile)</name>
  <files>app/page.tsx, app/(auth)/login/page.tsx, app/(auth)/register/page.tsx, app/(auth)/forgot-password/page.tsx, app/(auth)/reset-password/page.tsx</files>
  <action>
Read each page first. These pages have no data loading (no skeletons needed for data fetch), but need typography, spacing, button, and mobile polish.

**Home page (`app/page.tsx`):**
- Apply typography scale: hero heading text-2xl (or larger if hero -- can use text-3xl sm:text-4xl for hero specifically since it's the landing), section headings text-lg font-semibold
- Spacing: consistent section spacing
- Replace inline button styles with Button component for primary CTA buttons
- For `<Link>` elements styled as buttons (e.g., "Get Started", "Browse Courses"): apply the Button component's class pattern manually (rounded-lg font-medium + variant colors) -- do NOT wrap in Button component since they're Links
- Mobile: ensure hero text doesn't overflow, CTA buttons are full-width on mobile (w-full sm:w-auto), feature cards stack vertically
- Ensure the page looks consistent with the dashboard style (same indigo primary, same zinc neutrals, same rounded-lg)

**Auth pages (login, register, forgot-password, reset-password):**
- Apply typography scale: page/card title text-2xl font-bold, labels text-sm font-medium, body text-sm
- Spacing: form fields space-y-4, inner card p-6 sm:p-8
- Replace inline button styles with Button component:
  - Submit buttons: `<Button variant="primary" type="submit">` -- full width (className="w-full")
  - Link-style text ("Forgot password?", "Sign up"): keep as Link, apply text-sm font-medium text-indigo-600 hover:text-indigo-500
- Inputs: ensure consistent h-10 or h-11 height, rounded-lg, border-zinc-300 dark:border-zinc-700
- Mobile: auth cards should be nearly full-width on mobile (max-w-md mx-auto is fine), inputs and buttons should be easily tappable (44px min height -- inputs are already likely fine at h-10/h-11)
- reset-password page: replace the spinner (noted in research at line 193) with a skeleton or simple loading state using Skeleton primitives

**Final sweep across all 5 files:**
- Verify no `animate-spin` or `border-b-2 border-indigo-600` spinner patterns remain
- Verify consistent dark mode classes on all elements
- Verify consistent rounded-lg on all buttons and inputs
  </action>
  <verify>
    <automated>cd /home/rajiv/Personal/Projects/active/LMS && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Home page and all 4 auth pages have consistent typography, spacing, and button styling. Reset-password spinner replaced. No animate-spin patterns remain in any file in the app. All Link-styled-as-button elements use consistent rounded-lg font-medium classes.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- No `animate-spin` patterns remain in any page file across the entire app (verify with grep)
- Home page CTA buttons use consistent Button styling
- Auth page submit buttons use Button component
- Public pages visually match dashboard style (same color palette, typography, spacing)
- Final verification command: `grep -r "animate-spin" app/ --include="*.tsx" | grep -v "node_modules" | grep -v "ContentGenerationSkeleton"` returns empty (only ContentGenerationSkeleton is exempt -- but it uses animate-pulse not animate-spin, so this should be clean)
</verification>

<success_criteria>
- Explore and course preview pages show skeletons during route transitions
- Home page has consistent typography and button styling
- Auth pages use Button component for submit buttons
- Reset-password spinner eliminated
- No spinning circle loaders remain anywhere in the app (except ContentGenerationSkeleton which uses pulse, not spin)
- Public pages have same visual language as dashboard (indigo primary, zinc neutrals, rounded-lg)
- All pages mobile-friendly with 44px touch targets
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-polish/06-05-SUMMARY.md`
</output>
