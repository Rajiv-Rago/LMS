---
phase: 03-stabilization-bug-fixes
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/models/Course.ts
  - app/api/courses/[id]/enroll/route.ts
  - app/api/courses/route.ts
  - app/api/courses/[id]/route.ts
  - app/api/courses/[id]/assignments/route.ts
  - app/api/courses/[id]/assignments/[assignmentId]/route.ts
  - app/api/courses/[id]/assignments/[assignmentId]/files/route.ts
  - app/api/courses/[id]/assignments/[assignmentId]/submissions/route.ts
  - app/api/courses/[id]/modules/route.ts
  - app/api/courses/[id]/grades/route.ts
  - app/api/courses/[id]/gradebook/route.ts
  - app/api/ai/chat/route.ts
  - app/api/ai/generate/route.ts
  - app/api/ai/generate/[contentId]/route.ts
  - lib/auth/courseOwnership.ts
  - app/api/users/me/delete/route.ts
  - app/api/users/me/export/route.ts
  - __tests__/integration/courses/enrollment.test.ts
autonomous: true
requirements: [BUGS-01]

must_haves:
  truths:
    - "Enrolling in a course creates an Enrollment document, not an array push"
    - "Concurrent enrollment attempts do not create duplicate enrollments (compound index enforces uniqueness)"
    - "All routes that check enrollment status query the Enrollment collection, not course.enrolledStudents"
    - "GET /api/courses returns enrolled courses by querying Enrollment collection"
    - "Unenrolling deletes the Enrollment document"
    - "Course detail endpoint returns enrolledCount from Enrollment.countDocuments instead of array length"
  artifacts:
    - path: "app/api/courses/[id]/enroll/route.ts"
      provides: "Enrollment via Enrollment collection"
      contains: "Enrollment.create"
    - path: "__tests__/integration/courses/enrollment.test.ts"
      provides: "Integration tests for enrollment refactor"
      min_lines: 80
  key_links:
    - from: "app/api/courses/[id]/enroll/route.ts"
      to: "lib/models/Enrollment.ts"
      via: "Enrollment.create() for enroll, Enrollment.deleteOne() for unenroll"
      pattern: "Enrollment\\.(create|deleteOne)"
    - from: "app/api/courses/route.ts"
      to: "lib/models/Enrollment.ts"
      via: "Enrollment.find() to get user's enrolled course IDs"
      pattern: "Enrollment\\.find"
    - from: "lib/auth/courseOwnership.ts"
      to: "lib/models/Enrollment.ts"
      via: "Enrollment.exists() replacing enrolledStudents.some()"
      pattern: "Enrollment\\.exists"
---

<objective>
Migrate all enrollment logic from the Course.enrolledStudents array to the new Enrollment collection. This eliminates the enrollment race condition (TOCTOU bug) and scaling concern.

Purpose: The enrolledStudents array has a known race condition and does not scale. The compound unique index on the Enrollment collection makes duplicate enrollment impossible at the database level.
Output: All 16 files referencing enrolledStudents migrated to use Enrollment model. Enrollment integration tests rewritten.
</objective>

<execution_context>
@/home/rajiv/.claude/get-shit-done/workflows/execute-plan.md
@/home/rajiv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-stabilization-bug-fixes/03-CONTEXT.md
@.planning/phases/03-stabilization-bug-fixes/03-RESEARCH.md
@.planning/phases/03-stabilization-bug-fixes/03-01-SUMMARY.md

@app/api/courses/[id]/enroll/route.ts
@app/api/courses/route.ts
@app/api/courses/[id]/route.ts
@lib/models/Course.ts
@lib/auth/courseOwnership.ts
@__tests__/integration/courses/enrollment.test.ts

<interfaces>
<!-- From Plan 01 outputs -->

From lib/models/Enrollment.ts:
```typescript
export interface IEnrollment extends Document {
  course: mongoose.Types.ObjectId;
  student: mongoose.Types.ObjectId;
  enrolledAt: Date;
  createdAt: Date;
  updatedAt: Date;
}

// Static methods on the model:
// Enrollment.isEnrolled(courseId, studentId): Promise<boolean>
// Enrollment.getEnrollmentCount(courseId): Promise<number>
```

From lib/utils/validateObjectId.ts:
```typescript
export function validateObjectId(id: string, label?: string): NextResponse | null;
```

From lib/auth/coursePermissions.ts:
```typescript
export interface CoursePermissions {
  isInstructor: boolean;
  isEnrolled: boolean;
  isOwner: boolean;
  isAdmin: boolean;
  isSharedWith: boolean;
  canEdit: boolean;
  canView: boolean;
}
export function getCoursePermissions(course: ICourse, user: JWTPayload): Promise<CoursePermissions>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate enrollment route and rewrite enrollment tests</name>
  <files>
    app/api/courses/[id]/enroll/route.ts,
    __tests__/integration/courses/enrollment.test.ts
  </files>
  <behavior>
    - POST /api/courses/:id/enroll creates Enrollment document (not array push)
    - POST returns 400 "Already enrolled" when Enrollment already exists (catch E11000 or check first)
    - POST returns 400 for unpublished courses
    - POST returns 400 when instructor tries to enroll in own course
    - POST returns 401 for unauthenticated
    - POST returns 404 for non-existent course
    - POST returns 400 for invalid ObjectId
    - Concurrent enrollment attempts: only one succeeds (compound index)
    - DELETE /api/courses/:id/enroll removes Enrollment document
    - DELETE returns 400 "Not enrolled" when no Enrollment exists
    - DELETE returns 401 for unauthenticated
  </behavior>
  <action>
    Write failing tests first in __tests__/integration/courses/enrollment.test.ts, then fix.

    Rewrite the enrollment tests to use the Enrollment model:
    - Import createTestEnrollment from fixtures
    - Test POST enroll: creates Enrollment document, returns 200, verify Enrollment.exists()
    - Test POST duplicate: either catch MongoDB E11000 duplicate key error and return 400, or use Enrollment.exists() check first. Prefer try/catch on Enrollment.create() for atomicity -- the compound index guarantees uniqueness.
    - Test DELETE unenroll: removes Enrollment document via Enrollment.deleteOne()
    - Test concurrent enrollment: use Promise.all to call POST twice, verify only one Enrollment document exists

    Rewrite app/api/courses/[id]/enroll/route.ts:
    - Import Enrollment from "@/lib/models/Enrollment"
    - Import validateObjectId from "@/lib/utils/validateObjectId"
    - POST: Add validateObjectId(id) check at top. Replace enrolledStudents.some() with Enrollment.exists(). Replace push+save with Enrollment.create(). Wrap in try/catch -- if E11000, return 400 "Already enrolled".
    - DELETE: Replace enrolledStudents.filter()+save with Enrollment.deleteOne(). Check result.deletedCount to determine if was enrolled.
    - Remove all enrolledStudents references from this file.
    - Keep the notification to instructor on enrollment.
    - Keep cache invalidation.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="enrollment" --forceExit</automated>
  </verify>
  <done>
    - Enrollment POST creates Enrollment document, not array push
    - Duplicate enrollment prevented by compound index (returns 400)
    - Unenroll DELETE removes Enrollment document
    - Invalid ObjectId returns 400, not 500
    - All enrollment tests pass with new Enrollment collection
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining 14 files from enrolledStudents to Enrollment collection</name>
  <files>
    app/api/courses/route.ts,
    app/api/courses/[id]/route.ts,
    app/api/courses/[id]/assignments/route.ts,
    app/api/courses/[id]/assignments/[assignmentId]/route.ts,
    app/api/courses/[id]/assignments/[assignmentId]/files/route.ts,
    app/api/courses/[id]/assignments/[assignmentId]/submissions/route.ts,
    app/api/courses/[id]/modules/route.ts,
    app/api/courses/[id]/grades/route.ts,
    app/api/courses/[id]/gradebook/route.ts,
    app/api/ai/chat/route.ts,
    app/api/ai/generate/route.ts,
    app/api/ai/generate/[contentId]/route.ts,
    lib/auth/courseOwnership.ts,
    app/api/users/me/delete/route.ts,
    app/api/users/me/export/route.ts,
    lib/models/Course.ts
  </files>
  <action>
    Systematically replace every enrolledStudents reference across all remaining files:

    1. app/api/courses/route.ts (GET): Replace `{ enrolledStudents: user.userId }` filter with a two-step query: first get enrolled course IDs from `Enrollment.find({ student: user.userId }).distinct('course')`, then include those IDs in the Course query via `{ _id: { $in: enrolledCourseIds } }`. Merge with existing instructor/owner query using $or.

    2. app/api/courses/[id]/route.ts (GET): Replace `enrolledStudents.some()` with `await Enrollment.exists()`. Replace `enrolledStudents.length` with `await Enrollment.getEnrollmentCount()`. Add `enrolledCount` to the response instead of sending the full array. For course populate, remove enrolledStudents population if present.

    3. app/api/courses/[id]/route.ts (PATCH/DELETE): Replace `enrolledStudents.some()` with Enrollment query for authorization check.

    4. Routes that only check enrollment for authorization (all of these have the same pattern -- replace `course.enrolledStudents.some()` with `await Enrollment.exists({ course: id, student: user.userId })`):
       - app/api/courses/[id]/assignments/route.ts
       - app/api/courses/[id]/assignments/[assignmentId]/route.ts
       - app/api/courses/[id]/assignments/[assignmentId]/files/route.ts
       - app/api/courses/[id]/assignments/[assignmentId]/submissions/route.ts
       - app/api/courses/[id]/modules/route.ts
       - app/api/courses/[id]/grades/route.ts
       - app/api/ai/chat/route.ts
       - app/api/ai/generate/route.ts
       - app/api/ai/generate/[contentId]/route.ts

    5. app/api/courses/[id]/gradebook/route.ts: Replace enrollment list from course.enrolledStudents with `Enrollment.find({ course: id }).populate('student')`.

    6. lib/auth/courseOwnership.ts: Replace `course.enrolledStudents.some()` with `await Enrollment.exists({ course: courseId, student: user.userId })`. checkCourseOwnership already fetches the course, so it has the courseId available.

    7. app/api/users/me/delete/route.ts and export/route.ts: Replace any enrolledStudents references with Enrollment queries for user's enrollments.

    8. lib/models/Course.ts: Keep the enrolledStudents field in the schema for now (backward compatibility) but add a comment: "// DEPRECATED: Use Enrollment collection. This field retained for data migration only." Do NOT remove the field yet -- existing data may reference it.

    For every file: import Enrollment from "@/lib/models/Enrollment" where needed.
    Do NOT change the quiz route (Plan 03 handles that separately).
  </action>
  <verify>
    <automated>npm test -- --forceExit</automated>
  </verify>
  <done>
    - Zero remaining enrolledStudents.some() or enrolledStudents.push() calls in API routes
    - All enrollment checks use Enrollment collection
    - GET /api/courses returns enrolled courses via Enrollment.find().distinct()
    - Gradebook uses Enrollment.find().populate() for student list
    - courseOwnership.ts uses Enrollment.exists()
    - Course model enrolledStudents field marked deprecated
    - Full test suite passes
  </done>
</task>

</tasks>

<verification>
Run the full test suite to confirm enrollment migration is complete and nothing broke:
```bash
npm test -- --forceExit
```

Verify no remaining active usage of enrolledStudents (only deprecated schema field):
```bash
grep -r "enrolledStudents" app/api/ lib/auth/ --include="*.ts" | grep -v "DEPRECATED" | grep -v ".test."
```
</verification>

<success_criteria>
- All 16 files migrated from enrolledStudents array to Enrollment collection
- Race condition eliminated (compound unique index prevents duplicates)
- Enrollment integration tests pass with new collection
- No regressions in existing tests
- Course model enrolledStudents field marked deprecated but not removed
</success_criteria>

<output>
After completion, create `.planning/phases/03-stabilization-bug-fixes/03-02-SUMMARY.md`
</output>
