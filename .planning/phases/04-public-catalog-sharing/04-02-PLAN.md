---
phase: 04-public-catalog-sharing
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/(public)/layout.tsx
  - app/(public)/explore/page.tsx
  - app/(public)/courses/[id]/page.tsx
  - app/(public)/courses/[id]/CoursePreview.tsx
  - app/(public)/courses/[id]/opengraph-image.tsx
autonomous: true
requirements:
  - CATL-01
  - CATL-02
  - CATL-04

must_haves:
  truths:
    - "Unauthenticated user can visit /explore and see a card grid of published courses"
    - "User can search courses by keyword from the explore page search bar"
    - "Clicking a course card navigates to /courses/[id] showing a syllabus preview"
    - "Lesson content is locked -- visitor sees 'Enroll to access this lesson' on click"
    - "Course detail page shows enrollment count, creator attribution, and skill level badge"
    - "Sharing a course URL shows rich Open Graph preview with indigo gradient and course title"
    - "Load More button loads the next 12 courses"
  artifacts:
    - path: "app/(public)/layout.tsx"
      provides: "Minimal public header with logo, Sign in, Get Started buttons"
      min_lines: 20
    - path: "app/(public)/explore/page.tsx"
      provides: "Catalog page with hero search bar, card grid, Load More"
      min_lines: 80
    - path: "app/(public)/courses/[id]/page.tsx"
      provides: "Server component with generateMetadata for OG tags"
      exports: ["generateMetadata", "default"]
    - path: "app/(public)/courses/[id]/CoursePreview.tsx"
      provides: "Client component for interactive course preview with syllabus, enroll button"
      min_lines: 80
    - path: "app/(public)/courses/[id]/opengraph-image.tsx"
      provides: "Dynamic OG image with indigo gradient and course title"
      exports: ["default", "size", "contentType"]
  key_links:
    - from: "app/(public)/explore/page.tsx"
      to: "/api/courses?catalog=true"
      via: "fetch with catalog=true param"
      pattern: "catalog=true"
    - from: "app/(public)/courses/[id]/page.tsx"
      to: "/api/courses/[id]"
      via: "server-side DB query for metadata, passes ID to client component"
      pattern: "generateMetadata"
    - from: "app/(public)/courses/[id]/CoursePreview.tsx"
      to: "/api/courses/[id]"
      via: "client fetch for course data and permissions"
      pattern: "fetch.*api/courses"
---

<objective>
Create the public-facing route group with explore page (catalog) and course detail page with Open Graph metadata and dynamic OG image generation.

Purpose: This is the primary user-facing deliverable -- unauthenticated users can browse courses, preview syllabi, and see rich link previews when courses are shared.
Output: Public layout, explore page, course preview page, OG image generation.
</objective>

<execution_context>
@/home/rajiv/.claude/get-shit-done/workflows/execute-plan.md
@/home/rajiv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-public-catalog-sharing/04-CONTEXT.md
@.planning/phases/04-public-catalog-sharing/04-RESEARCH.md
@.planning/phases/04-public-catalog-sharing/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: updated types the executor needs -->

From lib/models/Course.ts (after Plan 01):
```typescript
export interface ICourse extends Document {
  _id: mongoose.Types.ObjectId;
  title: string;
  description: string;
  instructor: mongoose.Types.ObjectId;
  modules: mongoose.Types.ObjectId[];
  coverImage?: string;
  accessLevel: 'restricted' | 'unlisted' | 'published';
  enrolledCount: number;
  isPublished: boolean; // virtual: accessLevel !== 'restricted'
  owner?: mongoose.Types.ObjectId;
  sharedWith: mongoose.Types.ObjectId[];
  youtubeMetadata?: { skillLevel: string; teachingStyle?: string; pathVariant?: string; generatedAt: Date; };
  deletedAt: Date | null;
  createdAt: Date;
  updatedAt: Date;
}
```

From lib/auth/coursePermissions.ts (after Plan 01):
```typescript
export async function getCoursePermissions(course: ICourse, user: JWTPayload | null): Promise<CoursePermissions>;
```

From GET /api/courses (after Plan 01):
```
GET /api/courses?catalog=true -- returns published courses sorted by enrolledCount desc
GET /api/courses?catalog=true&search=keyword -- text search in catalog
GET /api/courses?catalog=true&page=2&limit=12 -- pagination
Response: { courses: Course[], pagination: { page, limit, total, totalPages } }
```

From GET /api/courses/[id] (after Plan 01):
```
Response: { course: CourseObject, permissions: { canEdit, canEnroll, isEnrolled, isInstructor } }
Course object includes: title, description, instructor.name, modules (with title, lessons), enrolledCount, youtubeMetadata, accessLevel
```

Existing landing page nav pattern (from app/page.tsx):
```typescript
<nav className="border-b border-zinc-200 dark:border-zinc-800 bg-white/80 dark:bg-zinc-950/80 backdrop-blur-sm sticky top-0 z-50">
  <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div className="flex justify-between h-16 items-center">
      <Link href="/" className="text-xl font-bold text-zinc-900 dark:text-white">Kantigo</Link>
      <div className="flex items-center gap-4">
        <Link href="/login" className="text-sm font-medium text-zinc-700 dark:text-zinc-300">Sign in</Link>
        <Link href="/register" className="px-4 py-2 text-sm font-medium text-white bg-indigo-600 rounded-md">Get Started</Link>
      </div>
    </div>
  </div>
</nav>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public route group layout and explore catalog page</name>
  <files>
    app/(public)/layout.tsx,
    app/(public)/explore/page.tsx
  </files>
  <action>
    **Create `app/(public)/layout.tsx`:**
    Minimal public layout with a sticky nav header matching the landing page nav style (reuse exact classes from app/page.tsx nav). Contains:
    - Logo "Kantigo" linking to `/`
    - "Explore" link (text link, not button) pointing to `/explore`
    - "Sign in" text link pointing to `/login`
    - "Get Started" indigo button linking to `/register`
    - `<main>{children}</main>` below nav
    - Background: `min-h-screen bg-zinc-50 dark:bg-zinc-950`
    - Dark mode support with same zinc scale used throughout the app

    This is a server component (no "use client"). It wraps all public pages.

    **Create `app/(public)/explore/page.tsx`:**
    This is a `"use client"` component. Layout:

    1. **Hero search section** at top:
       - Heading: "Explore courses" (text-3xl font-bold)
       - Large search input below the heading with placeholder "Search by topic, skill, or keyword..."
       - Search input has 300ms debounce (setTimeout/clearTimeout in useEffect)
       - On search input change, update URL search params with `search` query and reset page to 1

    2. **Card grid** below:
       - CSS grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6`
       - Max width container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
       - Each card shows:
         - Cover image fallback: indigo-to-violet gradient with first letter of title in white (56px font, centered) if no coverImage
         - If coverImage exists, show it with `object-cover` in a 16:9 aspect ratio container
         - Title (text-lg font-semibold, line-clamp-2)
         - Description snippet (text-sm text-zinc-600 dark:text-zinc-400, line-clamp-2)
         - Bottom row: lesson count (from modules[].lessons.length summed, or "N lessons" badge), enrollment count ("N learners" badge)
         - Skill level badge if available (from youtubeMetadata.skillLevel): small pill badge in amber
       - Card click navigates to `/courses/[id]`
       - Cards have `rounded-lg border border-zinc-200 dark:border-zinc-800 bg-white dark:bg-zinc-900 overflow-hidden hover:shadow-md transition-shadow`

    3. **Load More button** below grid:
       - Centered button: "Load more courses"
       - Only visible when there are more pages (pagination.page < pagination.totalPages)
       - On click, fetch next page and APPEND courses to existing list (not replace)
       - Button shows "Loading..." when fetching
       - Styling: `px-6 py-2 text-sm font-medium text-indigo-600 dark:text-indigo-400 border border-indigo-200 dark:border-indigo-800 rounded-md hover:bg-indigo-50 dark:hover:bg-indigo-950`

    4. **Empty state** when no courses found:
       - "No courses found for '[search term]'. Be the first to create one!"
       - CTA button: "Get Started" linking to `/register`
       - If no search term and no courses: "No courses available yet. Check back soon!"

    5. **Loading skeleton**: While initial fetch is loading, show 6 skeleton cards (pulsing zinc-200/zinc-800 blocks matching card layout).

    Data fetching: fetch from `/api/courses?catalog=true&page=${page}&limit=12${search ? '&search=' + encodeURIComponent(search) : ''}`. The API already handles auth via cookies (if logged in, excludes enrolled courses).

    State management: `courses` array (appended on Load More), `pagination` object, `search` string, `loading` boolean, `loadingMore` boolean.
  </action>
  <verify>
    <automated>cd /home/rajiv/Personal/Projects/active/LMS && npm run build 2>&1 | grep -E "(error|Error|failed|✓|✗|Compiled)" | head -10</automated>
  </verify>
  <done>
    /explore page renders with hero search bar, responsive card grid (3/2/1 columns), Load More pagination, empty states, and loading skeletons. Public layout has minimal header with logo, Explore link, Sign in, and Get Started buttons.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create public course detail page with generateMetadata, preview component, and OG image</name>
  <files>
    app/(public)/courses/[id]/page.tsx,
    app/(public)/courses/[id]/CoursePreview.tsx,
    app/(public)/courses/[id]/opengraph-image.tsx
  </files>
  <action>
    **IMPORTANT: Route conflict resolution.** The dashboard already has `app/(dashboard)/courses/[id]/page.tsx`. Two route groups cannot both serve `/courses/[id]`. Do NOT delete or move the dashboard course page in this plan -- it is used by other dashboard sub-routes (modules, assignments, ai, gradebook, grades). Instead, the public course page lives at a different URL: `/courses/[id]/preview`. Create the page at `app/(public)/courses/[id]/preview/page.tsx` and `app/(public)/courses/[id]/preview/CoursePreview.tsx` and `app/(public)/courses/[id]/preview/opengraph-image.tsx`.

    WAIT -- this conflicts with the user decision "Shareable URL = public detail page URL (`/courses/[id]`)". Since the user locked this URL, we must resolve the conflict properly. The correct approach:

    The dashboard route group at `(dashboard)/courses/[id]/page.tsx` owns `/courses/[id]`. For unauthenticated users, the dashboard layout redirects to `/login`. So unauthenticated users currently cannot see `/courses/[id]` at all. To make it work for both auth and unauth:

    **Convert the dashboard course detail page to a server component wrapper** that exports `generateMetadata`, and delegates to a client component for the interactive body. This avoids creating a route conflict. The page stays in `(dashboard)` but the layout must be updated to NOT redirect unauthenticated users on course detail pages.

    ACTUALLY -- this is too invasive for the dashboard layout. Simpler approach:

    **Create the public page at `app/(public)/explore/[id]/page.tsx`** (under the explore route). The shareable URL becomes `/explore/[id]`. Update all references to use `/explore/[id]` as the public course URL. The dashboard course URL `/courses/[id]` remains for authenticated users.

    BUT the user locked "Shareable URL = public detail page URL (`/courses/[id]`)".

    **Final resolution:** Create the public course detail page at `app/courses/[id]/page.tsx` (NOT inside any route group -- at the root app level). Root-level pages in Next.js App Router get the root layout. This avoids conflict with `(dashboard)` because route groups are transparent to URL resolution BUT a root-level `app/courses/[id]/page.tsx` and `app/(dashboard)/courses/[id]/page.tsx` WILL conflict.

    **Correct approach per Next.js docs:** Only ONE route segment can resolve to a given URL. Move the course detail page OUT of `(dashboard)` into `(public)`. The dashboard sub-routes (modules, assignments, etc.) can remain in `(dashboard)`. Only the top-level `page.tsx` for `/courses/[id]` moves. Authenticated users who visit `/courses/[id]` will see the public layout (minimal header, no sidebar) but the page content adapts based on auth state. This is acceptable -- the page shows full content for enrolled/owner users, preview for visitors.

    **Implementation:**

    1. **Move `app/(dashboard)/courses/[id]/page.tsx` to `app/(public)/courses/[id]/page.tsx`** -- actually, DO NOT move it. The dashboard has sub-routes under `courses/[id]/` (modules, assignments, etc.) that need the dashboard layout. Instead:
       - Delete `app/(dashboard)/courses/[id]/page.tsx` (the page.tsx only, NOT the directory or subdirectories)
       - Create `app/(public)/courses/[id]/page.tsx` as a new server component
       - The dashboard sub-routes at `(dashboard)/courses/[id]/modules/`, `(dashboard)/courses/[id]/assignments/`, etc. remain untouched -- they have their own page.tsx files and still get the dashboard layout

    2. **Create `app/(public)/courses/[id]/page.tsx`** (Server Component):
       - Export `generateMetadata` function that:
         - Awaits params to get id
         - Calls dbConnect(), fetches course by ID with populate('instructor', 'name')
         - Returns Metadata object: `{ title: "${course.title} | Kantigo", description: course.description.slice(0, 150), openGraph: { title, description, type: 'website' } }`
         - If course not found or restricted (and no auth), return `{ title: 'Course Not Found' }`
       - Default export renders `<CoursePreview courseId={id} />`
       - This is a server component -- NO "use client" directive

    3. **Create `app/(public)/courses/[id]/CoursePreview.tsx`** (Client Component):
       - `"use client"` directive
       - Props: `{ courseId: string }`
       - On mount, fetch `/api/courses/${courseId}` to get course data and permissions
       - Layout:
         - **Header section**: Course title (text-3xl font-bold), creator attribution ("Created by {instructor.name} with AI" if course has aiPreferences or syllabusStatus, else "Created by {instructor.name}"), skill level badge (amber pill) if youtubeMetadata.skillLevel exists, enrollment count badge ("{enrolledCount} learners enrolled")
         - **Description**: Full course description paragraph
         - **Syllabus section**: "Course syllabus" heading, then for each module:
           - Module name as collapsible accordion header (click to expand/collapse)
           - Inside accordion: list of lesson titles with type icons (document icon for text lessons, video/play icon for video lessons, quiz icon for quiz-type items)
           - Clicking a lesson title shows a small inline message "Enroll to access this lesson" (if not enrolled). If enrolled, link to `/courses/${courseId}/modules/${moduleId}/lessons/${lessonId}`
         - **Enroll CTA**: Large "Enroll for Free" button at top-right (desktop) or bottom sticky (mobile)
           - If not authenticated: navigates to `/login?enroll=${courseId}`
           - If authenticated and can enroll: POST to `/api/courses/${courseId}/enroll` with X-Requested-With header, then redirect to `/courses/${courseId}`
           - If already enrolled: shows "Continue Learning" button linking to `/courses/${courseId}`
           - If owner/instructor: shows "Edit Course" button
         - **Copy link button**: Small icon button or text button "Copy link" that copies `window.location.href` to clipboard using `navigator.clipboard.writeText()` and shows a toast "Link copied!"
       - Loading skeleton while fetching: pulsing blocks for title, description, and 3 module placeholders
       - Error state: if course not found or restricted, show "Course not found" with link back to /explore
       - Use toast from existing `components/ui/Toast.tsx` for clipboard confirmation

    4. **Create `app/(public)/courses/[id]/opengraph-image.tsx`**:
       - Import `ImageResponse` from `next/og`
       - Export `size = { width: 1200, height: 630 }` and `contentType = 'image/png'`
       - Default export: async function that awaits params, calls dbConnect(), fetches course title
       - Returns `new ImageResponse(...)` with:
         - Full-size div with `background: linear-gradient(135deg, #4f46e5, #7c3aed)` (indigo to violet)
         - Course title in white, 64px font-weight bold, centered, max 3 lines
         - "kantigo.dev" subtitle in light indigo (#c7d2fe), 28px, below title
         - All using inline styles (ImageResponse uses Satori which only supports inline CSS)
       - Fallback: if course not found, show generic "Kantigo" branded image

    **Dashboard link update:** The `(dashboard)/courses/[id]/` directory still has its sub-route pages (modules, assignments, etc.). Only the `page.tsx` is removed. Update any dashboard component that links to `/courses/${id}` to still work -- since the page now renders in the public layout, the link still works. Users on the dashboard clicking a course link will navigate to `/courses/${id}` which now renders in the public layout. The sidebar disappears on that page, which is acceptable per the research recommendation.
  </action>
  <verify>
    <automated>cd /home/rajiv/Personal/Projects/active/LMS && npm run build 2>&1 | grep -E "(error|Error|failed|✓|✗|Compiled|Route)" | head -15</automated>
  </verify>
  <done>
    /courses/[id] serves as the public course detail page with generateMetadata for OG tags. CoursePreview shows syllabus with type icons, enrollment count, creator attribution, skill level badge. Lessons are locked for unauthenticated visitors. Enroll button redirects to login with ?enroll param. OG image generates indigo gradient with course title. Build succeeds with no route conflicts.
  </done>
</task>

</tasks>

<verification>
Build and manual verification:
```bash
npm run build
```
Visit /explore to verify catalog renders. Visit /courses/[id] to verify OG tags in page source.
</verification>

<success_criteria>
- /explore shows a card grid of published courses with search and Load More
- Cards show cover image fallback, title, description, lesson count, enrollment count
- /courses/[id] shows course preview with syllabus, type icons, and enroll CTA
- Unauthenticated visitors see locked lessons with "Enroll to access" message
- generateMetadata returns correct OG title and description per course
- opengraph-image.tsx generates branded indigo gradient image with course title
- Copy link button copies URL and shows toast
- No build errors or route conflicts
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-catalog-sharing/04-02-SUMMARY.md`
</output>
